<form method="post" action="{{ path('stage_etudiant_delete', {'id': stage_etudiant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stage_etudiant.id) }}">
    <button class="btn">Delete</button>
</form>